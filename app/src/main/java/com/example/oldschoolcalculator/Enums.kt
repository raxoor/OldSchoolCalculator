package com.example.oldschoolcalculator

enum class LogicalOperators{
    AND,
    OR,
    XOR,
    NOT,
    NOR,
    NAND
}

enum class NumberBase{
    BIN,
    OCT,
    DEC,
    HEX
}

enum class BitWidth{
    BYTE,
    WORD,
    DWORD,
    QWORD
}

